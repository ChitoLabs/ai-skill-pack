---
description: Activate the Grill Me challenge interview
---

Use the `grill-me` skill now.

Treat `$ARGUMENTS` as the user's plan, proposal, assumptions, and requested challenge context. Load the skill by name through the host runtime's available skill mechanism and follow it exactly. This is explicit activation for hosts such as OpenCode, Pi, and Claude Code. If the skill is not registered, report that limitation. Do not locate it by path. Do not install it, copy it, or modify configuration.
